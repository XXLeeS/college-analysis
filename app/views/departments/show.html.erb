<div class="container">
	<div class="row">
		<h1><%= @department.name %></h1>
		<div class="col-md-4">
			<p>最低備取名次：<%= @department.last_text %></p>
			<p>系所領域：<%= @department.field_text %></p>
			<p>最低錄取分數：<%= @department.lowest_text %></p>
		</div>
		<div class="col-md-4">
			<div id="piechart">

			</div>
		</div>
	</div>
</div>