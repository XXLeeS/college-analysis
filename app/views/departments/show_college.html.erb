<div class="container" id="dep_showcollege">
	<h1><%= @college.name %></h1>
	<div class="row">
		<% @departments.each do |d| %>
		<div class="col-md-12">
			<%= link_to department_path(d) do %>
			<div class="row one-dep">
				<div class="col-xs-9">
					<span class="name"><%= d.short_name %></span>
				</div>
				<div class="col-xs-1 data">
					<p class="number"><%= "%.2f" % d.ts_rscore %></p>
					<p class="description">Rscore</p>
				</div>
				<div class="col-xs-1 data">
					<p class="number"><%= "%.2f" % d.ts_mu %></p>
					<p class="description">Mu</p>
				</div>
				<div class="col-xs-1 data">
					<p class="number"><%= "%.2f" % d.ts_sigma %></p>
					<p class="description">Sigma</p>
				</div>
			</div>
			<% end %>
		</div>
		<% end %>
	</div>
</div>